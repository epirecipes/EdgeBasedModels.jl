# Categorical Foundations of Edge-Based Compartmental Models

**Simon D.W. Frost**

## 1. Introduction

Edge-based compartmental models (EBCMs) and node-based compartmental models
(e.g., SIR) are two frameworks for modelling infectious disease on networks.
This document formalises the categorical relationship between them: the
**morphisms** connecting them, the **information lost** in translation, the
conditions under which the translation is exact, and the obstructions that
prevent an edge-based formulation.

We work in the setting of applied category theory, drawing on
Baez–Fong–Pollard's open Markov processes [1], Baez–Courser's
coarse-graining framework [2], and the compositional epidemiology programme
at the Topos Institute [3].

## 2. The Two Categories

### 2.1 Node-based models: **Node**

Objects are ODE systems on population-level state spaces.

- **State space:** Δ^{n-1} ⊆ ℝ^n (the probability simplex), with coordinates
  (S, I₁, …, I_M, R) summing to 1.
- **Dynamics:** dX/dt = f(X) where f encodes mass-action or frequency-dependent
  transmission. For SIR: dS/dt = -βSI, dI/dt = βSI - γI, dR/dt = γI.
- **Morphisms:** ODE-system maps (h: X → X') such that h intertwines the
  vector fields: Dh ∘ f = f' ∘ h (semi-conjugacy).

This is a category we call **Node** (or more precisely, a subcategory of
**ODE** restricted to epidemiological compartmental models).

### 2.2 Edge-based models: **Edge**

Objects are ODE systems on edge-probability state spaces, parameterised by
a probability generating function (PGF).

- **State space:** [0,1]^{K²(1+M)+K} with coordinates
  (θ_{jl}, φ_{m,jl}, R_l) for K population types and M disease stages.
- **PGF data:** A family of (multivariate) PGFs {ψ_j}_{j=1}^K encoding the
  degree distribution per type.
- **Dynamics:** The ODE system generated by the EBCM builder:
  - dθ_{jl}/dt = -Σ_m β_m · α_{jl} · φ_{m,jl}
  - dφ_{m,jl}/dt = (inflow)_m - (outflow)_m  with excess hazard terms
    involving ψ''_j / ψ'_j
  - φ_{S,jl} = ∂ψ_j/∂x_l(θ_j) / ∂ψ_j/∂x_l(1)  (algebraic)
- **Morphisms:** Maps of edge-state spaces that preserve the ODE structure
  and are compatible with the PGF data.

### 2.3 The ambient category

Both **Node** and **Edge** embed in the category **ODE** of smooth
dynamical systems on compact manifolds with corners, with semi-conjugacies
as morphisms.

## 3. The Coarse-Graining Functor F: Edge → Node

### 3.1 Definition

Given an EBCM object (θ, φ, R, ψ), define F on objects by:

```
F(θ, φ, R, ψ) = (S, I, R)  where
    S_l = ψ_l(θ_{1l}, …, θ_{Kl})        (evaluate PGF at θ-vector)
    R_l = R_l                              (identity)
    I_l = 1 - S_l - R_l                   (derived)
```

On morphisms, F sends an edge-level semi-conjugacy h to its induced
population-level semi-conjugacy F(h) = ψ ∘ h ∘ ψ^{-1} (where defined).

### 3.2 F is well-defined

**Proposition 3.1.** F preserves the ODE structure: if (θ(t), φ(t), R(t))
solves the EBCM system, then (S(t), I(t), R(t)) := F(θ(t), φ(t), R(t))
satisfies a closed ODE system on the simplex.

*Proof sketch.* The key computation:

dS_l/dt = Σ_j (∂ψ_l/∂x_j)(θ_l) · dθ_{jl}/dt

Substituting the EBCM θ-equation and using the algebraic relation
φ_{S,jl} = (∂ψ_l/∂x_j)(θ_l) / (∂ψ_l/∂x_j)(1), the right-hand side
becomes a function of S, I, R alone (plus the PGF-determined network
parameters). The resulting ODE is a generalised mass-action system. □

### 3.3 F is lossy (not faithful)

**Theorem 3.2.** F is not faithful: distinct EBCM trajectories can project
to identical node-level trajectories.

*Proof.* Consider two EBCMs with different PGFs ψ₁, ψ₂ but identical mean
degree κ and identical R₀. Choose ψ₁ = Poisson(κ), ψ₂ = bimodal with
same mean. Both yield the same S(0), I(0), R(0) and the same linearised
growth rate near the disease-free equilibrium, but differ in transient
dynamics. Thus ker(F) is non-trivial. □

**Theorem 3.3 (Information loss characterisation).** The fibre F⁻¹(S,I,R)
over a node-level trajectory is parameterised by the space of PGFs ψ with:
1. ψ(1) = 1 (normalisation)
2. ψ'(1) = κ (mean degree)
3. S(t) = ψ(θ(t)) for all t (consistency)

The **lost information** is precisely the degree distribution beyond its
first moment — i.e., the variance, skewness, and all higher cumulants.

## 4. The Lift G: Node → Edge (Right Adjoint)

### 4.1 Definition

Given a node-based SIR model with transmission rate β, recovery rate γ, and
basic reproduction number R₀ = βκ/(β+γ), define G by:

```
G(S, I, R; β, γ) = EBCM(ψ_Poisson(κ), β, γ)
    where κ = R₀(β+γ)/β
```

This embeds the node-based model into an EBCM with the **Poisson PGF** —
the unique PGF for which F ∘ G = id.

### 4.2 The Galois connection

**Theorem 4.1.** (F, G) form a Galois connection on the preorder of
epidemic models ordered by "refines" (i.e., M₁ ≤ M₂ iff M₁ can be
obtained from M₂ by coarse-graining):

```
F(E) ≤ N  ⟺  E ≤ G(N)
```

where ≤ means "is a coarse-graining of".

**Corollary 4.2.**
- F ∘ G = id_Node  (coarse-graining the Poisson lift recovers the original)
- G ∘ F ≠ id_Edge  (lifting then projecting forgets the original PGF)
- G ∘ F ∘ G = G     (the connection is idempotent)
- F ∘ G ∘ F = F     (the connection is idempotent)

### 4.3 Exactness of the Poisson section

**Theorem 4.3.** The following are equivalent:
1. F ∘ G(N) = N exactly (not just at equilibrium, but for all t)
2. The underlying network has a Poisson degree distribution
3. ψ''(1)/ψ'(1) = ψ'(1) (excess degree equals mean degree)
4. The degree distribution has variance equal to its mean

*Proof.* For Poisson(κ): ψ(z) = exp(κ(z-1)), so ψ'(z) = κψ(z),
ψ''(z) = κ²ψ(z), and ψ''(1)/ψ'(1) = κ = ψ'(1). The excess hazard
term in the EBCM reduces to β·κ·φ_S, which is exactly the mass-action
term β·S·I/N after rescaling. Conversely, if ψ''(1)/ψ'(1) ≠ ψ'(1),
the excess hazard introduces degree-heterogeneity corrections that have
no counterpart in the node-based model. □

## 5. When Can't We Use an EBCM? Obstructions to Edge-Based Modelling

The EBCM framework rests on specific assumptions. Category theory helps
identify the **obstructions** — conditions under which no valid object in
**Edge** maps to the desired epidemiological behaviour.

### 5.1 The configuration model assumption (tree-like local structure)

**Obstruction 1: Clustering / triangles.**

The EBCM assumes edges are **independent** conditional on degree — i.e.,
the network is drawn from a configuration model. This is equivalent to
requiring that the local neighbourhood of each node is tree-like.

Categorically, the EBCM functor F is only well-defined (i.e., produces a
closed ODE) when the PGF closure holds:

```
S(t) = ψ(θ(t))     (exact iff tree-like)
```

On networks with clustering (triangles, cliques), the joint probability of
edge states is NOT a product. The PGF closure fails because:

```
P(all edges of node v untransmitted) ≠ ∏_e θ_e
```

The correlations between edges sharing a node break the factorisation that
the PGF provides. In categorical terms, the would-be functor F fails to
preserve the ODE structure — dS/dt is no longer expressible as a function
of (S, I, R, θ, φ) alone; it depends on higher-order edge correlations.

**Formal statement:** Let **Edge_tree** be the full subcategory of **Edge**
on configuration-model networks. The functor F: **Edge_tree** → **Node** is
well-defined. The extension to **Edge_clustered** requires additional
structure (triangle-closing terms, Volz's clustering corrections) that
changes the category.

### 5.2 The Markov assumption (memoryless transitions)

**Obstruction 2: Non-exponential infectious periods.**

The EBCM ODEs assume that all disease-stage transitions are exponentially
distributed (constant hazard rates). For non-Markovian processes (e.g.,
fixed infectious period, gamma-distributed latency):

- The edge state φ_{m,jl}(t) is no longer sufficient; one needs the full
  age-since-infection distribution.
- The ODE system must be replaced by integro-differential equations or
  delay-differential equations.

Categorically, the state space [0,1]^n is insufficient — we need a
function space (the infection-age distribution), moving us from **ODE** to
**PDE** or **DDE**. The EBCM category **Edge** does not contain objects
with these dynamics.

### 5.3 The independence of initial conditions

**Obstruction 3: Correlated initial infections.**

The EBCM assumes the initial infection is placed uniformly at random
(each node independently infected with probability ε → 0). If the initial
condition is spatially correlated (e.g., a localised outbreak), the
factorisation θ(0) = 1 - ε breaks down for edges near the seed.

### 5.4 Summary: the domain of validity as a subcategory

```
Full stochastic on any graph    ← no EBCM (too general)
    ↓ restrict to config. model
Stochastic on config. model     ← EBCM exact (on trees, N→∞)
    ↓ restrict to Markovian
Markovian on config. model      ← EBCM with ODEs (our Edge category)
    ↓ restrict to Poisson degree
Markovian on Poisson network    ← equivalent to mass-action SIR
    ↓ forget network
Standard compartmental (Node)
```

Each restriction defines a **subcategory inclusion** with a forgetful
functor. The EBCM is valid precisely in the subcategory of Markovian
processes on configuration-model networks.

## 6. Open Systems and Composition

### 6.1 Node-based: structured cospans

In the Baez–Fong–Pollard framework, node-based models compose as
**structured cospans** in **FinSet**:

```
    A → X ← B
```

where X is the set of compartments, A and B are boundary compartments
(e.g., migration ports). Composition via pushout:

```
    A → X ← B → Y ← C    ↦    A → X ⊔_B Y ← C
```

### 6.2 Edge-based: multivariate PGF composition

EBCMs compose via the **multivariate PGF**:

```
    Type_l ←→ Type_j    via    ψ_j(x_1, …, x_K) and θ_{jl}
```

The composition data is:
- A set of types K
- Per-type PGFs {ψ_j}
- A contact matrix α_{jl} (coupling strengths)

This is NOT a structured cospan. Instead, it resembles composition in a
**decorated cospan** category where the decoration is the PGF data:

```
    Type_l → EdgeSpace_{lj} ← Type_j
    decorated by (ψ_l, ψ_j, α_{lj})
```

### 6.3 A conjectured adjunction of open systems

**Conjecture 6.1.** There exists a symmetric monoidal functor

```
    F_open: Open(Edge) → Open(Node)
```

from the category of open EBCM systems to open node-based systems, such
that:
1. F_open on closed systems (no boundary) agrees with F from §3
2. F_open preserves composition (is monoidal)
3. The Poisson section G lifts to G_open: Open(Node) → Open(Edge)
4. (F_open, G_open) form a monoidal Galois connection

This would unify the compositional frameworks and make precise the sense
in which edge-based models are "network-aware refinements" of node-based
models.

## 7. Going from Node to Edge: The Inverse Problem

### 7.1 Can we always lift?

**Given** a node-based model (S, I, R trajectories), can we find an EBCM
that reproduces them?

**Theorem 7.1.** For any well-posed SIR trajectory with S(0) = 1-ε, there
exists a one-parameter family of EBCMs indexed by PGFs ψ with ψ'(1) = κ
that reproduce the trajectory exactly.

*Proof.* Given S(t), define θ(t) = ψ⁻¹(S(t)) where ψ⁻¹ is the functional
inverse of ψ on [0,1] (which exists since ψ is strictly increasing on [0,1]
for any valid PGF with positive mean degree). Then R(t) is given, and the
φ variables are determined by the EBCM algebraic relations. □

**Caveat:** The lift is not unique — the choice of ψ is free (subject to
ψ'(1) = κ). Different PGFs give different edge-level dynamics that project
to the same population curve. The Poisson lift is canonical (minimal
information added).

### 7.2 When the lift fails

The lift G: Node → Edge fails when the node-based model violates EBCM
structural constraints:

1. **Negative θ:** If S(t) decays faster than any ψ(θ) allows (e.g.,
   super-exponential decline), no valid θ(t) ∈ [0,1] exists.
2. **Non-monotone θ:** In the EBCM, θ(t) is non-increasing (edges can only
   transmit, not "un-transmit"). If the node model requires θ to increase
   (e.g., waning immunity restoring susceptibility), no standard EBCM
   exists. This is why SIS requires special treatment (recovery restores
   edges).
3. **Multi-strain interference:** Node-based models with cross-immunity
   between strains introduce state dependencies that break the edge
   independence assumption.

## 8. The Degree-Variance Inequality

The categorical structure reveals a sharp **quantitative** criterion for
when the EBCM and node-based model diverge:

**Theorem 8.1 (Degree-variance inequality).** Let ψ be any valid PGF with
mean degree κ. Then:

```
    ψ''(1)/ψ'(1) = κ + (σ² - κ)/κ = κ + (σ²/κ - 1)
```

where σ² = Var(degree). The EBCM excess hazard exceeds the mass-action
rate by a factor of (1 + (σ²/κ - 1)/κ) = σ²/κ².

- σ² = κ (Poisson): excess hazard = mass-action rate (exact equivalence)
- σ² > κ (overdispersed): EBCM predicts faster epidemic, higher R₀
- σ² < κ (underdispersed): EBCM predicts slower epidemic, lower R₀

This is the **index of dispersion** (σ²/κ), and it is the single scalar
that measures "how much information the EBCM adds over the node model."

## References

[1] Baez, Fong, Pollard. "A compositional framework for Markov processes."
    J. Math. Phys. 57, 033301 (2016). arXiv:1508.06448

[2] Baez, Courser. "Coarse-graining open Markov processes."
    Theory and Applications of Categories 33, 1223–1268 (2018).
    arXiv:1710.11343

[3] Libkind, Baas, Halter, Patterson, Fairbanks. "An algebraic framework
    for structured epidemic modelling." Phil. Trans. R. Soc. A 380 (2022).

[4] Miller. "A note on a paper by Erik Volz: SIR dynamics in random
    networks." J. Math. Biol. 62, 349–358 (2011).

[5] Miller, Slim, Volz. "Edge-based compartmental modelling for infectious
    disease spread." J. R. Soc. Interface 9, 890–906 (2012).

[6] Fong, Spivak. "Seven Sketches in Compositionality: An Invitation to
    Applied Category Theory." Cambridge University Press (2019).
